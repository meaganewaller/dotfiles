-- https://github.com/tamton-aquib/zone.nvim

-- { == Configuration ==> =====================================================
local zone = require("zone")

zone.setup({
  style = "dvd",
  after = 30,
  dvd = {
    tick_time = 100,
  },
})

-- <== }
