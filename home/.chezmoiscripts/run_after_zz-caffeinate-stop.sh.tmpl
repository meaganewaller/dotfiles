#!/usr/bin/env bash
set -eo pipefail

{{ if eq .chezmoi.os "darwin" -}}
if [ -f /tmp/chezmoi-caffeinate.pid ]; then
  kill "$(cat /tmp/chezmoi-caffeinate.pid)" 2> /dev/null || true
  rm -f /tmp/chezmoi-caffeinate.pid
fi
{{ end -}}
