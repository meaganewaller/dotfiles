packages:
  darwin:
    brews:
      # Security & Auth
      - cosign
      - git-crypt
      - gnupg
      - ca-certificates

      # AI Tools
      - aichat

      # CLI Utilities
      - awscli
      - coreutils
      - curl
      - mise
      - pkl-lsp
      - tree

      # System Monitoring & Performance
      - ccusage
      - htop

      # Terminal Multiplexing & Shells
      - tmux
      - zsh

      # Docker & Containerization
      - devcontainer
      - docker
      - docker-buildx
      - docker-completion
      - docker-compose
      - docker-credential-helper
      - docker-credential-helper-ecr

      # Media & Compression
      - ffmpeg
      - flac
      - lz4
      - lzo
      - xz
      - zstd

      # Git & Version Control
      - gh
      - git
      - git-lfs
      - worktrunk

      # Documentation & Visualization
      - mermaid-cli
      - pandoc
      - texinfo
    casks:
      # Development Tools
      - cursor
      - ngrok
      - visual-studio-code

      # Communication
      - slack

      # Notes
      - obsidian

      # Media
      - vlc

      # Security & Authentication
      - 1password-cli
      - 1password

      # Audio
      - loopback

      # Fonts
      - font-maple-mono-nf-cn

      # Terminals
      - ghostty
  linux:
    dnf:
      # Core Tools
      - ca-certificates
      - gh
      - git
      - git-lfs
      - htop
      - neovim
      - tmux
      - util-linux-user
      - zsh
