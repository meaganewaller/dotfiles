packages:
  darwin:
    brews:
      # Security & Auth
      - cosign
      - git-crypt
      - gnupg
      - ca-certificates

      # AI Tools
      - aichat
      - ccusage

      # CLI Utilities
      - awscli
      - coreutils
      - curl
      - emojify
      - mise
      - pkl-lsp
      - tree

      # System Monitoring & Performance
      - htop

      # Terminal Multiplexing & Shells
      - tmux
      - zsh

      # Menu Bar
      - FelixKratz/formulae/sketchybar
      - lua # interpreter for the SbarLua-driven sketchybarrc (ABI-matches SbarLua's vendored Lua)

      # Docker & Containerization
      - devcontainer
      - docker
      - docker-buildx
      - docker-completion
      - docker-compose
      - docker-credential-helper
      - docker-credential-helper-ecr

      # Media & Compression
      - ffmpeg
      - flac
      - lz4
      - lzo
      - xz
      - zstd

      # Git & Version Control
      - gh
      - git
      - git-lfs
      - worktrunk

      # Documentation & Visualization
      - mermaid-cli
      - pandoc
      - texinfo

      # MacOS Tooling
      - antoniorodr/memo/memo
      - steipete/tap/remindctl
    casks:
      # Development Tools
      - cursor
      - ngrok
      - visual-studio-code

      # Communication
      - slack

      # Notes
      - obsidian

      # Media
      - vlc

      # Security & Authentication
      - 1password-cli
      - 1password

      # Audio
      - loopback

      # Fonts
      - font-maple-mono-nf-cn
      - font-pixelify-sans
      - font-departure-mono

      # Terminals
      - ghostty
  linux:
    dnf:
      # Core Tools
      - ca-certificates
      - gh
      - git
      - git-lfs
      - htop
      - neovim
      - tmux
      - util-linux-user
      - zsh
