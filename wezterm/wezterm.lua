local wezterm = require 'wezterm'

return {
	tab_max_width = 16,
	hide_bar_if_only_one_tab = false,
	font_size = 16,
	window_background_opacity = 0.97,
	font = wezterm.font('Fira Code'),
	color_scheme = 'Catppuccin Mocha',
	tab_bar_at_bottom = true,
}
