/docs/gitingest
AGENTS.local.md
CLAUDE.local.md
home/dot_claude/skills/skill-optimization-workspace/
.claude/scheduled_tasks.lock

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
# Transient lock bd leaves in the worktree root; shows up as an untracked file
# after any bd command and is never worth committing.
.beads.gate.lock
# Derived from the database and conflict-prone; see docs/beads.md.
.beads/interactions.jsonl
# Generated per-project; hooks come from init.templateDir instead.
.beads/hooks/

# mise writes npm provenance-verification scratch files here on every
# `mise lock` run for npm-backend tools (e.g. markdownlint-cli). They are
# not part of mise.lock and were observed inflating hk's file-count plan
# when left untracked in the working tree.
.mise/locks/
