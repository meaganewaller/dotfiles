# --- MacOS Base Configuration ---
- ifmacos:
    - bootstraphomebrew:
    - brew:
        - asdf
    - link:
        ~/Brewfile: package-managers/homebrew/Brewfile
        /usr/local/bin/brewup: package-managers/homebrew/brewup

    - brewfile:
      - ~/Brewfile

    - shell:
      - [brewup, Updating Homebrew and install programs]
