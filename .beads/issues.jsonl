{"_type":"issue","id":"dotfiles-1eb","title":"claude-chill alias shadows the claude() safety-wrapper function in zsh","description":"home/.chezmoidata/aliases.yaml defines a custom entry (tool: claude-chill, replaces: claude, command: 'claude-chill -- claude'), rendered in home/dot_zshrc.tmpl:121-125 as alias claude='claude-chill -- claude'. Separately, home/dot_zshrc.tmpl:250-257 defines a claude() function specifically to work around an --add-dir variadic-argument bug that swallows subcommands (per the comment at line 244-249). In zsh, alias expansion of a bare command word happens before function lookup, so on any machine with claude-chill installed the alias always wins and the claude() function's fix is never reached. Fix: either drop the claude-chill alias, rename it, or fold the claude-chill wrapping into the claude() function itself so both concerns are handled in one place.","status":"closed","priority":1,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:11Z","created_by":"Meagan Waller","updated_at":"2026-07-27T20:22:58Z","started_at":"2026-07-27T18:55:24Z","closed_at":"2026-07-27T20:22:58Z","close_reason":"Removed the claude-chill custom alias from aliases.yaml (it always won over the claude() function in zsh, silently disabling the --add-dir variadic-argument fix) and folded claude-chill wrapping into the claude() function itself via a template-computed $claudeCmd prefix, gated by the same lookPath check. Verified via chezmoi execute-template + zsh -n; full BATS run blocked by an unrelated sandbox limitation (bash here-strings hang in this session for any .bats file).","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-zwj","title":"lazygit AI-commit custom command has stray trailing quote, breaks the G binding","description":"home/dot_config/lazygit/config.yml.tmpl:15 has command: \"~/.config/lazygit/aicommit.sh'\" — a stray trailing single-quote inside the double-quoted string. The real rendered script is ~/.config/lazygit/aicommit.sh (no quote), from home/dot_config/lazygit/executable_aicommit.sh. The extra character means lazygit's AI Commit (G) custom command fails to find/execute the script. Fix: remove the stray '.","status":"closed","priority":1,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:10Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:26:48Z","started_at":"2026-07-27T16:26:00Z","closed_at":"2026-07-27T16:26:48Z","close_reason":"Removed stray trailing single-quote in home/dot_config/lazygit/config.yml.tmpl:15 that broke the aicommit.sh path; verified via chezmoi execute-template that the rendered command now points to the correct script.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-ei9","title":"Renovate has zero coverage for home/dot_config/mise/config.toml.tmpl","description":"renovate.json5's mise manager (line 30-34) is scoped to managerFilePatterns: ['/^mise\\.toml$/'] -- the root repo-tooling file only (bats/gitleaks/shellcheck/etc). It does not cover home/dot_config/mise/config.toml.tmpl, the actual user dev-environment config with 100+ pinned tool versions (aqua/github/npm/pipx-backed CLIs, language servers, MCP servers). docs/renovate.md explicitly documents 5 custom managers that should cover this file -- 'Custom Managers' items 2-5 (aqua-prefixed, github-prefixed, npm-prefixed, pipx-prefixed tools in mise TOML) plus the base mise manager -- with worked examples like 'aqua:mikefarah/yq' and 'npm:@anthropic-ai/claude-code'. None of these customManagers actually exist in renovate.json5 (verified: zero matches for aqua:/github:/npm:/pipx: patterns in the file's customManagers block). Even if they did exist, the documented path 'home/dot_config/mise/config.toml' is itself wrong -- the real file is config.toml.tmpl (a Chezmoi template), so a manager targeting the undotted name would never match the real file either way. Net effect: every version pin in this large, security/reproducibility-relevant file (mise.lock's source of truth) gets zero automated Renovate PRs today, contradicting docs/renovate.md, docs/package-management.md's Quick Reference table (marks this file 'Yes' for Renovate coverage), and docs/adrs/0003-mise-config-plus-lockfile.md's stated model. Fix: add the 4 missing aqua/github/npm/pipx customManagers scoped to the correct .tmpl path (and fix the base mise manager's pattern to also include it), matching the docs' own worked examples -- this is squarely Package Manager subagent / Renovate config territory per AGENTS.md.","status":"closed","priority":1,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T19:19:42Z","created_by":"Meagan Waller","updated_at":"2026-07-28T14:44:31Z","started_at":"2026-07-27T20:27:26Z","closed_at":"2026-07-28T14:44:31Z","close_reason":"Added 5 custom regex managers to renovate.json5 (aqua/github/ubi/npm/pipx-prefixed tools) scoped to home/dot_config/mise/config.toml.tmpl, since that file is a Chezmoi template (invalid TOML) that Renovate's native mise manager can never parse regardless of managerFilePatterns -- confirmed left the base mise: manager untouched intentionally, documented why. currentValue patterns require a leading digit so 'latest' entries (the deliberate ADR-0003 policy) are never touched. Fixed the wrong undotted config.toml path across docs/renovate.md, docs/package-management.md, docs/agents/chezmoi.md, README.md, docs/adrs/0003-mise-config-plus-lockfile.md, .claude/agents/package-manager.md, home/dot_claude/skills/install-tool/SKILL.md, and .claude/skills/install/SKILL.md (the last of which had a functional bug: its allowed-tools Edit() permission targeted the nonexistent undotted filename). Validated by parsing renovate.json5 as JS/JSON5, compiling every matchStrings regex, and running the 5 new managers against the real config file -- confirmed correct extraction of the 2 currently-pinned entries (aqua:pnpm/pnpm, ubi:miltonparedes/kitmux) and 0 false matches against latest/inline-table entries. Work was started by a Package Manager subagent (renovate.json5 + the 3 docs it touched) that died to a connection error before closing the issue; I verified its output and finished the remaining stale-path cleanup myself.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-xeo","title":"install: cosign signature verification for chezmoi download is broken (upstream moved to Sigstore bundles)","description":"./install downloads chezmoi from GitHub releases (home/../install lines ~813-869) when apt/brew can't provide it. With the default VERIFY_SIGNATURES=true, it downloads chezmoi_\u003cversion\u003e_checksums.txt.sig and chezmoi_cosign.pub and runs 'cosign verify-blob --signature ... --key ...'. As of chezmoi v2.71.1, upstream no longer publishes a .sig/.pub pair for checksums.txt -- confirmed via the GitHub Releases API, the actual assets are chezmoi_\u003cversion\u003e_checksums.txt.sigstore.json (a Sigstore bundle) plus per-artifact .sbom.json files. The old .sig filename now 404s every time, so any fresh install path that can't satisfy chezmoi via apt/brew (e.g. Debian/Ubuntu, which doesn't package chezmoi at all, including inside a plain container) fails outright with VERIFY_SIGNATURES=true. sha256 checksum verification (verify_checksum, unconditional) still runs fine and is unaffected -- only the extra cosign signature-of-the-checksums-file step is broken. Likely also affects CI's cold-start.yml/ci.yml on ubuntu-24.04 runners, since bootstrap-dotfiles/action.yml never sets VERIFY_SIGNATURES=false and apt has no chezmoi package there either -- worth confirming whether CI is currently green only by luck (e.g. a cached/pre-installed chezmoi) or is quietly broken too. Fix should update install's cosign invocation to verify the .sigstore.json bundle (Sigstore keyless verification: cosign verify-blob --bundle chezmoi_\u003cversion\u003e_checksums.txt.sigstore.json --certificate-identity-regexp ... --certificate-oidc-issuer https://token.actions.githubusercontent.com, matching whatever identity/issuer chezmoi's release workflow actually uses) instead of the retired detached-signature scheme. Found while building a new root Dockerfile (dotfiles-au4) -- worked around there by pinning VERIFY_SIGNATURES=false with a comment pointing at this issue, since fixing the verification scheme itself is out of scope for that change.","status":"closed","priority":1,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T16:26:51Z","created_by":"Meagan Waller","updated_at":"2026-07-27T21:36:38Z","started_at":"2026-07-27T20:27:26Z","closed_at":"2026-07-27T21:36:38Z","close_reason":"Fixed ./install's download_chezmoi to verify chezmoi's Sigstore bundle (chezmoi_\u003cversion\u003e_checksums.txt.sigstore.json) via 'cosign verify-blob --bundle ... --key chezmoi_cosign.pub ...' instead of the retired detached .sig scheme. Confirmed via chezmoi's .config/goreleaser.yaml that it's still key-based signing (same cosign.key), just wrapped as a Sigstore bundle -- no OIDC/keyless verification needed. Removed the VERIFY_SIGNATURES=false workaround from Dockerfile now that real verification works. Validated end-to-end: (1) isolated shell harness sourcing download_chezmoi against the real v2.71.1 release, both with signatures on ('Verified OK') and off; (2) full Docker build of the Ubuntu container (no chezmoi/cosign apt package, forcing the GitHub-release+cosign fallback) succeeded with VERIFY_SIGNATURES defaulting to true, and the resulting image has a working goreleaser-built chezmoi binary.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-k7p","title":"run_onchange_install-nvim-plugins.sh.tmpl is stale after the vim.pack migration","description":"home/.chezmoiscripts/run_onchange_install-nvim-plugins.sh.tmpl was written for lazy.nvim and has two compounding problems now that Neovim config uses the native vim.pack manager instead (confirmed via vim.pack.add() calls in home/dot_config/nvim/plugin/*.lua, and nvim-pack-lock.json as the lock file -- see dotfiles-7gl):\n\n1. The onchange re-fire hash is `(glob \"dot_config/nvim/lua/plugins/*.lua\") | join \"\" | sha256sum`, but home/dot_config/nvim/lua/plugins/ no longer exists (confirmed: `ls` fails). The glob always returns zero matches, so the hash is the constant sha256 of the empty string (\"e3b0c4...b855\") on every render. This means the script's change-detection is permanently broken -- it will never re-fire due to a plugin config change again, defeating its stated purpose (\"automatically syncs Neovim plugins when any plugin config changes\").\n\n2. Even if it did fire, it runs `nvim --headless \"+Lazy! sync\" +qa` -- `Lazy` is lazy.nvim's user command, which isn't loaded under vim.pack. If nvim is present and CI/GITHUB_ACTIONS aren't set, this would hit `set -o errexit` behavior in whatever way nvim handles the unrecognized command (needs to be checked empirically -- may error the whole chezmoi apply, or may be a silent no-op depending on nvim's behavior for unknown ex commands via --headless).\n\nDiscovered while adding BATS test coverage for this script (dotfiles-sye) -- the new tests in test/run_onchange_install-nvim-plugins.bats faithfully cover the CURRENT (broken) behavior (constant hash, calls nvim with the Lazy command) without asserting the hash value itself is meaningful, specifically to avoid baking in the bug as \"expected.\"\n\nFix: decide whether this script should be retired entirely (vim.pack has its own update mechanism / nvim-pack-lock.json is the source of truth, per dotfiles-7gl) or repointed at a real vim.pack equivalent (e.g. re-glob against wherever vim.pack.add() calls now live -- home/dot_config/nvim/plugin/*.lua -- and replace \"+Lazy! sync\" with whatever vim.pack's headless update command is, if one exists).","status":"closed","priority":2,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-28T17:14:45Z","created_by":"Meagan Waller","updated_at":"2026-07-28T18:39:19Z","started_at":"2026-07-28T18:26:44Z","closed_at":"2026-07-28T18:39:19Z","close_reason":"Rewrote run_onchange_install-nvim-plugins.sh.tmpl for vim.pack instead of retiring it, since vim.pack has no automatic non-interactive sync of its own -- something still needs to invoke it.\n\nRoot-caused the hash bug more precisely than the original report: it wasn't just that dot_config/nvim/lua/plugins/ no longer exists. `glob` requires an ABSOLUTE path (confirmed empirically: `glob \"dot_config/nvim/plugin/*\"` on a real existing directory returns `[]`, while `glob (joinPath .chezmoi.sourceDir \"dot_config/nvim/plugin/*\")` on the same directory returns real matches) -- the old `glob \"dot_config/nvim/lua/plugins/*.lua\"` call used a relative pattern, so it would have silently returned empty even before the directory was removed. Separately, `glob` returns file *paths*, not contents, so even a working glob would only re-fire on add/remove/rename, never on an in-place edit to an existing plugin file.\n\nFix, verified against the real nvim 0.12.2 install locally (bypassing the mise shim's own unrelated trust-check bug to call the real binary directly):\n- New hash: `joinPath`-qualified glob over dot_config/nvim/plugin/*.lua (where vim.pack.add() calls actually live now), reading each matched file's *content* via `include` and concatenating, plus `include`-ing nvim-pack-lock.json itself so a Renovate-driven pin bump also re-fires this script. Empirically confirmed the hash changes on a plugin-file content edit, on a lockfile rev bump, and is stable (idempotent) across repeated renders with no change.\n- New nvim invocation: `nvim --headless -c \"lua vim.pack.update(nil, { force = true, target = 'lockfile' })\" +qa`, replacing the dead `Lazy! sync`. Read the real `:help vim.pack` from the installed nvim to get this right rather than guessing: `vim.pack.add()` (called automatically by sourcing plugin/*.lua on any startup, headless or not) already installs any not-yet-on-disk plugin at the lockfile-pinned revision; `vim.pack.update(nil, { force = true, target = 'lockfile' })` additionally brings already-installed plugins in line with the lockfile (e.g. after a Renovate bump) without vim.pack's normal interactive confirmation buffer, which would otherwise hang a non-interactive run. Verified end-to-end against a real GitHub repo (nvim-lua/plenary.nvim) in an isolated XDG sandbox: fresh install from empty state (exit 0, plugin cloned at the lockfile-declared rev), and force-downgrade-then-resync of an already-installed plugin to a different pinned rev (exit 0, correct rev after). Also independently confirmed the old bad command (`+Lazy! sync`) does NOT hang or errexit the whole apply -- nvim prints E492 and still exits 0 -- softening (but not eliminating) point 2 of the original report; moot now regardless since the command is fixed.\n\nUpdated test/run_onchange_install-nvim-plugins.bats: fixed the CI/no-nvim/DEBUG/failure-propagation tests' render() helper to point sourceDir at the real repo (it previously pointed at an empty scratch dir, which would have made the *new* hash's glob half silently empty too -- the same bug class, caught before it shipped). Added two new regression tests, against a small mutable fixture tree, proving the hash changes on a plugin-file content edit and on a lockfile bump -- directly targeting the bug class that caused this ticket. Updated the sync-invocation assertion to match the new vim.pack.update command. All 10 tests in that file pass; full suite is 103/103 via ./bin/test.\n\nAlso fixed two doc references to the pre-vim.pack setup that were still describing this exact stale mechanism (found via grep for \"LazyVim\"/\"lua/plugins\" while investigating): README.md's tech-stack table said \"LazyVim / lazy.nvim\"; docs/package-management.md's package-location table and installation-scripts table both said \"home/dot_config/nvim/lua/plugins/*.lua\" / \"LazyVim specs\" / \"Via LazyVim\". Both now describe vim.pack + nvim-pack-lock.json, and the Renovate-coverage column for Neovim plugins is now accurately \"Yes\" (per dotfiles-7gl's fix).","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-zpg","title":"mise config.toml.tmpl has dozens of 'latest' pins; documented custom Renovate managers for it don't exist","description":"home/dot_config/mise/config.toml.tmpl has ~150 tool entries, dozens pinned to \"latest\" (e.g. around lines 42, 62-63, 67, 70-90), contradicting docs/package-management.md's stated philosophy ('Pins explicit versions (no latest)' / 'Immutable Pins Over Latest'). Separately, docs/renovate.md and docs/package-management.md:8-11 describe custom regex Renovate managers for aqua:/github:/npm:/pipx:-prefixed entries inside this file, but no such customManagers entries exist anywhere in renovate.json5 (the built-in mise manager there only matches the repo's own ./mise.toml dev-tool manifest, not the deployed config). Net effect: the deployed mise tool list has essentially zero real Renovate coverage today, contrary to what the docs claim. Fix: either add the documented custom managers for home/dot_config/mise/config.toml.tmpl, or update the docs to match reality, and replace 'latest' pins with explicit versions where feasible.","status":"closed","priority":2,"issue_type":"task","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:15Z","created_by":"Meagan Waller","updated_at":"2026-07-28T15:04:45Z","started_at":"2026-07-28T15:04:44Z","closed_at":"2026-07-28T15:04:45Z","close_reason":"Both halves of this issue were resolved as a side effect of dotfiles-ei9: (1) the documented aqua/github/npm/pipx custom Renovate managers for home/dot_config/mise/config.toml.tmpl now actually exist in renovate.json5 (plus a bonus ubi: manager); (2) docs/package-management.md's Core Philosophy was rewritten from 'Immutable Pins Over Latest' to 'Reproducible Resolution' -- explicit versions OR latest-backed-by-a-committed-lockfile -- so the dozens of intentional 'latest' pins (policy per ADR 0003) no longer contradict the stated philosophy; they're now documented as the expected/correct state rather than a gap. Bulk-converting the ~150 entries to explicit pins was one of two alternative fixes the issue offered ('either add the documented custom managers... or update the docs to match reality') -- we did the latter (plus the former), which fully satisfies the fix criteria without the version-bump risk of touching every tool pin. Verified by re-reading both docs files post-ei9.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-dnd","title":"zsh/fish chezmoi externals fetch unpinned branch tarballs, contradicting SHA-pinning docs","description":"home/.chezmoiexternals/zsh.toml.tmpl fetches oh-my-zsh, zsh-autosuggestions, and zsh-syntax-highlighting from archive/master.tar.gz (branch tarball, not a commit SHA). home/.chezmoiexternals/fish.toml.tmpl fetches chezmoi/watchexec completions and tokyonight themes from master/main branch raw URLs. docs/renovate.md explicitly claims these externals are 'pinned to commit SHAs for reproducibility' with regex rules matching a hex digest ([a-f0-9]{7,40}) in the URL, but the literal URL text is master/main, so the doc is inaccurate and (separately from the wrong file path in dotfiles-t8u) the regex wouldn't match a digest here even once the path is fixed. Fix: either pin these externals to actual commit SHAs (matching the documented convention) or correct docs/renovate.md to describe the real unpinned state. Related to dotfiles-t8u (wrong file path in the same Renovate managers).","status":"closed","priority":2,"issue_type":"task","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:14Z","created_by":"Meagan Waller","updated_at":"2026-07-28T15:26:53Z","started_at":"2026-07-28T15:23:21Z","closed_at":"2026-07-28T15:26:53Z","close_reason":"Re-pinned all 4 unpinned URLs in home/.chezmoiexternals/fish.toml.tmpl (chezmoi completions, watchexec completions, and 4 tokyonight.nvim theme files sharing one repo/SHA) from floating master/main branch refs to verified commit SHAs, matching the zsh/ghostty pattern already fixed in dotfiles-t8u. Verified each resolved SHA via gh api repos/\u003cowner\u003e/\u003crepo\u003e/commits/\u003cbranch\u003e --jq .sha, then confirmed the exact file still exists at that SHA via HTTP HEAD against raw.githubusercontent.com for all 6 URLs (200 for all). Added 3 new customManagers to renovate.json5 (twpayne/chezmoi, watchexec/watchexec, folke/tokyonight.nvim) so these SHAs get automated future updates -- this file previously had zero Renovate coverage. Validated via node eval (JSON5 + regex compile) and by running the new regexes against the real post-edit file: chezmoi and watchexec each match once, tokyonight.nvim matches all 4 occurrences (correctly grouped into one future PR since they share depName). Validated the rendered TOML via chezmoi execute-template + yq -p toml. Documented the 3 new rules in docs/renovate.md's Chezmoi Externals section without attempting the full singular-vs-plural path sweep tracked separately by dotfiles-6oq.","dependencies":[{"issue_id":"dotfiles-dnd","depends_on_id":"dotfiles-t8u","type":"blocks","created_at":"2026-07-27T12:24:28Z","created_by":"Meagan Waller","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-rxb","title":"fish config never activates mise on work-profile machines","description":"home/dot_config/fish/config.fish.tmpl:11-28 — the 'mise activate fish --shims | source' block (and the cargo PATH prepend) is only inside the {{ else }} (personal-profile) branch. On work_profile = true machines using fish, mise is never activated at all, so mise-managed tool shims won't resolve. Fix: move the mise activation (and cargo PATH line, if it should apply to both profiles) outside the profile conditional, or add it to the work-profile branch too.","status":"closed","priority":2,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:12Z","created_by":"Meagan Waller","updated_at":"2026-07-28T15:06:43Z","started_at":"2026-07-28T15:04:57Z","closed_at":"2026-07-28T15:06:43Z","close_reason":"Moved the mise activation block (and cargo PATH prepend) out of the {{ else }} (personal-profile) branch in home/dot_config/fish/config.fish.tmpl so it runs unconditionally after the work/personal PROFILE branch, instead of only on personal-profile machines. Verified by rendering the template with both work_profile=true and work_profile=false via chezmoi execute-template -- mise activate fish --shims | source now appears in both outputs -- and syntax-checked the rendered work-profile output with fish --no-execute.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-d9h","title":"run_onchange_00-install-mise-tools can run before mise itself is installed on a fresh machine","description":"home/.chezmoiscripts/run_onchange_00-install-mise-tools.sh.tmpl is numerically prefixed to run early and requires mise on PATH (checks command -v mise), but mise itself is installed via Homebrew by run_onchange_install-packages-darwin.sh.tmpl (home/.chezmoidata/packages.yaml brews list), whose filename sorts alphabetically after 00-install-mise-tools. On a first-ever chezmoi apply, the mise-tools script runs first, finds no mise, and skips — and because it's run_onchange_ (content-hash gated, not run_before_/run_after_), it won't automatically re-fire on the next apply since the tracked config/lockfile content hasn't changed. Global mise tools can end up never installed until the user happens to edit mise/config.toml or the lockfile again. Fix: rename to sort after the brew install script, or make it run_onchange_after_ relative to the package install, or add an explicit dependency check that retries.","status":"closed","priority":2,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:11Z","created_by":"Meagan Waller","updated_at":"2026-07-28T15:17:11Z","started_at":"2026-07-28T15:04:57Z","closed_at":"2026-07-28T15:17:11Z","close_reason":"Changed run_onchange_00-install-mise-tools.sh.tmpl's missing-mise guard from 'exit 0' (silent skip) to 'exit 1' (real failure). Root cause: this script is run_onchange_ (content-hash gated), and chezmoi only records a script's hash as successfully-applied on exit 0 -- exiting 0 when mise isn't found yet (e.g. on a fresh machine where install-packages-darwin/linux, which installs mise itself, sorts after this script alphabetically) permanently marks the step done and it never retries until the tracked mise config/lockfile hash changes again. Verified the retry semantics with an isolated standalone chezmoi source/destination experiment (unrelated to the real repo): a run_onchange_ script that exits 1 gets re-run on every subsequent chezmoi apply with an unchanged hash, while one that exits 0 does not -- confirming this fix gives genuine self-healing retry behavior rather than a permanent skip. Verified the actual rendered script separately (mise excluded from PATH, run directly, no chezmoi apply involved) now exits 1 with a clear message instead of silently exiting 0. Note: an earlier test step in this session accidentally ran real 'mise install'/'mise prune' against the live environment (not properly isolated) and pruned one stale, already-unreferenced uv version -- flagged to the user directly; confirmed harmless via mise prune --dry-run showing nothing further pending.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-7tk","title":"tmux-plugins.yaml jsonata Renovate manager queries fields that don't exist in the file","description":"renovate.json5's jsonata customManager for tmux plugins (~line 182-192) uses the query: tmuxPlugins.{ \"depName\": repo, \"currentValue\": ref, \"currentDigest\": commit } against home/.chezmoidata/tmux-plugins.yaml. But that file's actual structure (confirmed by reading it) has root key tmux_plugins (snake_case, not tmuxPlugins) containing an extras array of objects shaped { path, type, url, revision } -- none of repo/ref/commit exist anywhere in the file. The jsonata query evaluates to an empty result, so Renovate silently never checks the three pinned tmux plugin revisions (tmux-sensible, tmux-powerline, tmux-prefix-highlight) for updates, despite docs/adrs/0002-tmux-plugins-via-chezmoi-externals.md explicitly claiming Renovate 'can target stable file + field shapes in YAML for digest/SHA bumps' for this exact file. Fix: correct the jsonata query to match the real shape, e.g. tmux_plugins.extras.{ \"depName\": url, \"currentDigest\": revision } (adjusting depName extraction since url is a full git URL, not an owner/repo shorthand like the other jsonata/regex managers use).","status":"closed","priority":2,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T19:19:54Z","created_by":"Meagan Waller","updated_at":"2026-07-28T15:21:14Z","started_at":"2026-07-28T15:04:55Z","closed_at":"2026-07-28T15:21:14Z","close_reason":"Fixed the jsonata customManager query to match tmux-plugins.yaml's real shape: tmux_plugins.extras.{ depName (owner/repo via $replace-stripped url), currentValue: branch, currentDigest: revision } instead of the old tmuxPlugins.{repo,ref,commit} query that matched zero real fields. Added a per-entry branch field to the YAML since erikw/tmux-powerline's default branch is main while the other two are master (verified via gh api repos/\u003cowner\u003e/\u003crepo\u003e --jq .default_branch for all three) -- a single hardcoded currentValueTemplate would silently miss tmux-powerline. Independently verified: JSON5+regex validity via node eval, hand-simulated the $replace($replace(...)) depName extraction in plain JS against the real 3 YAML entries (all 3 produce correct depName/currentValue/currentDigest/packageName), and confirmed all three branch values against GitHub's actual default_branch. renovate-config-validator/renovate CLI unavailable locally to run the literal extract dry-run. Work done by a Package Manager subagent (no Bash access); I ran the validation and bd close on its behalf after independently re-verifying its claims.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-t8u","title":"4 Renovate customManagers target a nonexistent home/.chezmoiexternal.toml.tmpl path","description":"renovate.json5 has 4 customManagers (catppuccin/ghostty ~line 117, ohmyzsh/ohmyzsh ~130, zsh-autosuggestions ~143, zsh-syntax-highlighting ~156) all scoped to managerFilePatterns: ['/^home/\\.chezmoiexternal\\.toml\\.tmpl$/'] -- a singular file that no longer exists. Externals were split into home/.chezmoiexternals/ (plural directory: fish.toml.tmpl, ghostty.toml, sketchybar-*.toml.tmpl, tmux.toml.tmpl, zsh.toml.tmpl). The sketchybar-related customManagers were correctly updated to this new layout (~line 87, ~170); these 4 were not. They can never match any file, so these 4 pins are never checked for updates.\\n\\nFixing the path alone isn't enough for 3 of the 4: home/.chezmoiexternals/zsh.toml.tmpl currently pins ohmyzsh, zsh-autosuggestions, and zsh-syntax-highlighting to literal 'archive/master.tar.gz' URLs -- a floating branch reference, not a commit SHA -- so even a corrected regex (which expects a 7-40 char hex digest) wouldn't match. This also contradicts docs/renovate.md's 'All externals pinned to commit SHAs' claim and this repo's own stated pinning policy (AGENTS.md / Package Manager subagent: 'pin tarball to commit SHA'). The 4th, home/.chezmoiexternals/ghostty.toml, is already correctly pinned to a commit SHA (5a58926563ddacbde4a12b4a347464c2c6945393) -- only its manager's file path needs fixing.\\n\\nFix: update all 4 managers' managerFilePatterns to the correct plural-directory paths, and separately pin the 3 zsh-related externals to commit SHAs (matching ghostty.toml's existing pattern) so the corrected regex actually has something to match.","status":"closed","priority":2,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T19:19:49Z","created_by":"Meagan Waller","updated_at":"2026-07-28T15:21:14Z","started_at":"2026-07-28T15:04:56Z","closed_at":"2026-07-28T15:21:14Z","close_reason":"Repointed all 4 dead customManagers (catppuccin/ghostty, ohmyzsh, zsh-autosuggestions, zsh-syntax-highlighting) from the nonexistent singular home/.chezmoiexternal.toml.tmpl to the real plural-directory paths (home/.chezmoiexternals/ghostty.toml, home/.chezmoiexternals/zsh.toml.tmpl). Re-pinned the 3 zsh-related externals from floating archive/master.tar.gz URLs to commit SHAs matching ghostty.toml's existing pattern -- independently verified all 3 SHAs against gh api repos/\u003cowner\u003e/\u003crepo\u003e/commits/master --jq .sha, all matched exactly. Confirmed via grep that renovate.json5 no longer references the singular chezmoiexternal.toml.tmpl path anywhere. Work done by a Package Manager subagent (no Bash access); I ran validation and bd close on its behalf after independently re-verifying the SHAs and path fixes myself.","dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"dotfiles-gyp","title":"packages.yaml: .packages.linux has no apt list, only dnf — Debian/Ubuntu users get zero packages from run_onchange_install-packages-linux.sh","description":"home/.chezmoidata/packages.yaml defines packages.linux.dnf (ca-certificates, gh, git, git-lfs, htop, neovim, tmux, util-linux-user, zsh) but has no packages.linux.apt list at all. home/.chezmoiscripts/run_onchange_install-packages-linux.sh.tmpl templates '(index .packages.linux \"apt\")' with a 'default list' fallback, so on any apt-based system (Debian, Ubuntu, WSL Ubuntu, containers) the apt branch runs apt-get install -y with an empty package list -- meaning gh/git-lfs/htop/neovim/tmux/util-linux-user/jq never get installed via this path on apt systems today, bare metal or container. Discovered while building the new Dockerfile (dotfiles-au4): jq specifically caused a hard failure there (bin/sync-claude-extras exits 1 without it once claude is installed), worked around locally by adding jq directly to the Dockerfile's own apt-get install line since the container guard in that script assumes packages are already baked into the image. A proper fix should add a packages.linux.apt list mirroring the dnf one (package names may differ slightly, e.g. util-linux-user has no direct apt equivalent -- verify per package) so bare-metal Debian/Ubuntu users aren't silently missing these tools too.","status":"closed","priority":2,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T16:53:59Z","created_by":"Meagan Waller","updated_at":"2026-07-28T15:12:45Z","started_at":"2026-07-28T15:04:58Z","closed_at":"2026-07-28T15:12:45Z","close_reason":"Added packages.linux.apt to home/.chezmoidata/packages.yaml mirroring the dnf list (ca-certificates, gh, git, git-lfs, htop, neovim, tmux, zsh), fixing run_onchange_install-packages-linux.sh.tmpl's apt-get install -y branch which previously ran with zero packages (index .packages.linux \"apt\" returned nil, default list fell back to empty). Omitted util-linux-user with an explanatory comment: verified via apt-cache policy in a real ubuntu:24.04 container that it has no apt equivalent, and its user-management utilities (chsh, newgrp, etc.) already ship in Debian/Ubuntu's default util-linux/passwd/login packages (confirmed already 'Installed' on a bare image), unlike Fedora/RHEL where dnf's package needs explicit install. Verified the other 8 package names all resolve to real apt candidates in the same container, validated the YAML with yq, and rendered+shellchecked the full script template with the real data to confirm the apt-get install -y line now lists all 8 packages with correct bash syntax.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-b73","title":"install: install_chezmoi's own PATH check runs before add_to_path, always fails on GitHub-fallback path in non-interactive shells","description":"install_chezmoi() (install lines ~887-939) downloads+installs chezmoi to $BIN_DIR via download_chezmoi(), then immediately checks 'command -v chezmoi' (line ~934) to verify success. But $BIN_DIR (default ~/.local/bin) is only added to PATH by add_to_path(), which main() doesn't call until line ~1014 -- AFTER run_logged_step chezmoi (line ~1012) has already returned. Since install_chezmoi runs inside run_logged_step's subshell, its own command -v check always runs before PATH has been updated, so it always fails whenever chezmoi is installed via the GitHub-release fallback (i.e. whenever apt/brew can't provide chezmoi, e.g. on Debian/Ubuntu, which doesn't package chezmoi at all). This is masked on interactive shells where ~/.local/bin is typically already on PATH via ~/.profile/~/.zprofile before ./install even runs, but is a hard, deterministic failure in any non-interactive context that doesn't pre-seed PATH (confirmed in a fresh container). Interestingly, install_chezmoi's own check (lines 933-938, 'chezmoi installation failed - binary not found in PATH', hard exit 1) is redundant with -- and less robust than -- the proper fallback resolution main() already does 80 lines later (lines ~1016-1029: probes command -v, falls back to checking /chezmoi and other absolute-path candidates directly, regardless of PATH). Fix: either remove/relax install_chezmoi's premature command -v check (checking file existence at /chezmoi instead, since download_chezmoi already mv+chmod's it unconditionally), or call add_to_path earlier before this check runs. Found and worked around at the Dockerfile level (dotfiles-au4) by pre-seeding ENV PATH to include the container user's ~/.local/bin before running ./install -- that's a reasonable container-specific accommodation, but the underlying ordering bug affects any non-interactive ./install invocation, not just containers.","status":"closed","priority":2,"issue_type":"bug","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T16:47:53Z","created_by":"Meagan Waller","updated_at":"2026-07-28T15:09:04Z","started_at":"2026-07-28T15:04:58Z","closed_at":"2026-07-28T15:09:04Z","close_reason":"install_chezmoi()'s final verification checked 'command -v chezmoi', which always fails on the GitHub-fallback path in non-interactive shells since $BIN_DIR isn't added to PATH until add_to_path() runs later in main() -- well after install_chezmoi returns. Since download_chezmoi already exit-1's internally on any real download/verify failure, reaching this check means the binary is guaranteed to exist at $BIN_DIR/chezmoi; changed the check to test file existence there directly instead of relying on PATH (main() already does its own more thorough PATH-independent candidate search afterward, so no need to duplicate that here). Verified with two isolated shell-harness runs against the real v2.71.1 release, PATH deliberately excluding $BIN_DIR and with a sanity check confirming chezmoi was truly unresolvable via command -v: the pre-fix code failed with exit 1 ('binary not found in PATH') despite a working binary sitting right at $BIN_DIR/chezmoi; the post-fix code returns exit 0.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-au4","title":"Containerize dotfiles dev environment with a Dockerfile","description":"Add a Dockerfile (and likely docker-compose or devcontainer config) so the dotfiles repo can be built into a container image, letting anyone spin up a fully-configured dev environment without running ./install on bare metal. Should reuse the existing chezmoi-based install flow rather than duplicating package lists.","acceptance_criteria":"A Dockerfile exists at the repo root (or docker/ dir) that: 1) builds an image with mise + chezmoi + the tools this repo installs, 2) applies the chezmoi source so the resulting ~ matches what's committed, 3) is documented in README.md with build/run instructions. CI or a manual smoke test confirms the image builds and a shell inside it reflects the dotfiles config.","notes":"Fully validated end to end: docker build completes cleanly (real chezmoi version resolves via the install fix, no hang), docker run -it drops into a working zsh shell with correct git identity from build args, and 'bin/setup; bin/test' passes all 73 bats tests inside the built image. hk check shows no new lint regressions (shellcheck/markdownlint/gitleaks/jq all clean on touched files; the one ghalint failure on cold-start.yml predates this work). Not verified: opening via VS Code/Cursor 'Reopen in Container' interactively (devcontainer.json is valid JSON and follows the spec, but wasn't exercised in an actual editor), and the new docker.yml workflow going green on a real PR (can't push from here). Both are reasonable to confirm once this lands.","status":"closed","priority":2,"issue_type":"feature","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T15:13:36Z","created_by":"Meagan Waller","updated_at":"2026-07-24T17:42:36Z","started_at":"2026-07-24T16:13:32Z","closed_at":"2026-07-24T17:42:36Z","close_reason":"Dockerfile + .devcontainer/devcontainer.json + docker.yml CI job implemented and validated (build + full test suite pass). Four unrelated pre-existing bugs found along the way tracked separately: dotfiles-xeo, dotfiles-b73, dotfiles-gyp, dotfiles-f14.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-7gl","title":"nvim-pack-lock Renovate manager's currentValueTemplate: 'HEAD' likely never resolves digest updates","description":"renovate.json5's nvim-pack-lock.json customManager (~line 262-271, git-refs datasource) sets currentValueTemplate: 'HEAD' instead of a real branch/tag name. Renovate's git-refs docs describe digest-only updates as 'put the named reference in currentValue and match on currentDigest' -- implying a real ref name under refs/heads or refs/tags, not the symbolic HEAD. Every other git-refs customManager in this same file (ghostty, ohmyzsh, zsh-autosuggestions, zsh-syntax-highlighting, tmux-plugins) uses a real branch name (master/main) rather than HEAD, which is the established working pattern in this repo. Discovered as a side effect of fixing dotfiles-t8u/7tk (same file, same datasource) by a Package Manager subagent; not independently confirmed against Renovate's actual source or a live extract run (renovate-config-validator/renovate CLI unavailable locally). Fix: investigate whether HEAD actually resolves for git-refs digest lookups (may need to check Renovate's source or file an upstream question), and if not, determine each locked repo's actual default branch (like the zsh/tmux fixes did) and set currentValueTemplate accordingly per entry, or restructure via a jsonata manager if the packageName varies per entry the way tmux-plugins.yaml's does.","status":"closed","priority":3,"issue_type":"bug","owner":"meagan.waller@gusto.com","created_at":"2026-07-28T15:22:17Z","created_by":"Meagan Waller","updated_at":"2026-07-28T16:18:58Z","closed_at":"2026-07-28T16:18:58Z","close_reason":"Confirmed the HEAD bug against Renovate's actual git-refs datasource source (lib/modules/datasource/git-refs/index.ts getDigest): when newValue is passed (Renovate always passes currentValue as newValue for digest lookups), it only matches refs with type 'heads'/'tags'; the real HEAD entry has type '' and is only matched when newValue is absent entirely. So currentValueTemplate: 'HEAD' can never resolve a digest -- confirmed, not just 'likely'.\n\nSince nvim-pack-lock.json is generated/rewritten by Neovim's vim.pack manager (confirmed via vim.pack.add() usage in home/dot_config/nvim/plugin/*.lua), it can't carry a per-entry branch field the way tmux-plugins.yaml does (that file is repo-owned). Converted the customManager from regex to jsonata: $each(plugins, ...) iterates the plugins object and looks up each repo's real default branch from a hardcoded table (all 26 repos' owner/repo -\u003e default_branch verified via `gh api repos/\u003cowner\u003e/\u003crepo\u003e --jq .default_branch`; mix of main/master, e.g. kanagawa.nvim/lualine.nvim/melange-nvim/neverland-vim-theme/nvim-web-devicons/oil.nvim/onedark.nvim/vim-afterglow are master, rest are main).\n\nVerified: (1) renovate.json5 parses as valid JSON5 via a locally cached json5 module; (2) ran the real renovate-config-validator binary (found cached under ~/.npm/_npx from a prior npx run) against renovate.json5 -- validated successfully; (3) evaluated the literal JSONata expression via a locally cached jsonata@2.2.1 library against the real nvim-pack-lock.json content -- produced all 26 correct {depName, packageName, currentDigest, currentValue} entries matching the file and the verified default branches. Known limitation documented in a code comment: a new plugin added to nvim-pack-lock.json without a matching table entry will silently lack currentValue (no digest tracking) until the table is updated -- inherent to the lock file having no branch data of its own.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-sye","title":"4 of 10 chezmoi scripts under home/.chezmoiscripts have no BATS test coverage","description":"test/*.bats covers run_onchange_configure-macos-preferences, run_onchange_install-mise-tools, run_onchange_install-packages-darwin, and run_onchange_install-packages-linux, but there are no tests for run_once_install-claude-code.sh, run_onchange_after_install-sketchybar-lua.sh.tmpl, run_onchange_after_verify-theme-registry.sh.tmpl, or run_onchange_install-nvim-plugins.sh.tmpl. The theme-registry script in particular is meant to be a validation gate and currently has no regression coverage. Fix: add BATS tests for these four scripts following the existing patterns in test/test_helper.bash.","status":"closed","priority":3,"issue_type":"task","assignee":"Meagan Waller","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:18Z","created_by":"Meagan Waller","updated_at":"2026-07-28T17:15:06Z","started_at":"2026-07-28T16:58:17Z","closed_at":"2026-07-28T17:15:06Z","close_reason":"Added BATS coverage for all 4 previously-untested chezmoiscripts, following the existing render-with-chezmoi-execute-template / stub-and-run patterns in test/test_helper.bash and the other test/*.bats files:\n\n- test/run_once_install-claude-code.bats (4 tests): valid shell structure, CI skip without invoking curl, already-installed skip without invoking curl, and a real curl|bash install path exercised with a fully isolated PATH (env -i) so it can't accidentally find a real claude/curl on the host.\n- test/run_onchange_after_install-sketchybar-lua.bats (8 tests): darwin-only rendering, CI skip, missing chezmoi-external-source skip, missing-make skip, successful build (stubbed make creates the .so), and the exit-1 path when make succeeds but the module never appears. Needed --source (not just --config's sourceDir data value) since the template's hash comment calls `include`, which chezmoi always resolves against the real -S/--source flag.\n- test/run_onchange_after_verify-theme-registry.bats (8 tests): this is the \"validation gate\" the issue specifically called out as needing regression coverage. Built a trimmed real-file fixture (only the specific files the template's hardcoded include list needs, skipping the 28MB unrelated bulk of dot_config) and exploited the fact that .chezmoi.sourceDir (template *data*, settable via --config) and the real --source flag (used by `include`) are independently controllable to test the missing-applier-dir and empty-applier-dir failure paths without needing a second full fixture tree. Also covered: missing-applier-in-palette and orphan-applier-in-directory failure paths (via yq -i mutation / touch), state-file-driven vs default-driven theme seeding, and a \"the real repo registry is currently self-consistent\" regression canary that will fail if the real repo's theme.d appliers and themes.yaml palette ever drift apart.\n- test/run_onchange_install-nvim-plugins.bats (8 tests): valid shell structure, template idempotence, CI skip, missing-nvim skip, successful sync (asserts nvim invoked with `--headless +Lazy! sync +qa`), failure propagation when nvim exits non-zero, and DEBUG=1 verbose output. Deliberately did NOT assert the hash comment's literal value, since (discovered while writing this) the glob path it hashes no longer exists post-vim.pack-migration, making the hash a constant -- filed as a separate bug (dotfiles-k7p) rather than baking the broken behavior into a passing assertion.\n\nAlso fixed a latent, previously-unexercised bug in test/test_helper.bash's assert_script_structure: it only substring-matched '#!/bin/bash' / '#!/bin/sh', which would have failed for any script using '#!/usr/bin/env bash' -- exactly the shebang style used by 2 of these 4 target scripts (theme-registry, nvim-plugins) and already used elsewhere in the repo (bin/sync-claude-extras, bin/sync-claude-settings). Extended it to also accept the env-indirected forms; verified against both old and new shebang styles before and after.\n\nVerified: all 28 new tests pass individually and the full suite (101 tests, up from 73) passes via `./bin/test` (the same entrypoint CI uses), with no regressions to the pre-existing 73.\n\nSide finding filed separately as dotfiles-k7p (not fixed here, out of scope for a test-coverage task): run_onchange_install-nvim-plugins.sh.tmpl's onchange hash and its `Lazy! sync` invocation are both stale relative to the vim.pack migration.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-6oq","title":"Sweep docs/ADRs referencing single-file home/.chezmoiexternal.toml.tmpl to the real .chezmoiexternals/ directory","description":"AGENTS.md/CLAUDE.md, .claude/agents/package-manager.md, docs/package-management.md, docs/renovate.md, docs/tmux.md, and ADRs 0002/0006/0009 all reference a single home/.chezmoiexternal.toml.tmpl file, but the real layout is a directory: home/.chezmoiexternals/ containing fish.toml.tmpl, ghostty.toml, sketchybar-app-font.toml.tmpl, sketchybar-lua.toml.tmpl, tmux.toml.tmpl, and zsh.toml.tmpl. This drift is the root cause of dotfiles-t8u (Renovate customManagers targeting the nonexistent path). Fix: either rename the directory back to a single file, or do a repo-wide sweep of docs/ADRs and renovate.json5 to reference the real .chezmoiexternals/*.toml.tmpl paths. Should be done together with dotfiles-t8u.","status":"open","priority":3,"issue_type":"task","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:16Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:24:16Z","dependencies":[{"issue_id":"dotfiles-6oq","depends_on_id":"dotfiles-t8u","type":"blocks","created_at":"2026-07-27T12:24:27Z","created_by":"Meagan Waller","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-1yg","title":"ADR 0002 and tmux-plugins.yaml still reference a removed gpakosz/.tmux external","description":"home/.chezmoidata/tmux-plugins.yaml:1-2 and docs/adrs/0002-tmux-plugins-via-chezmoi-externals.md describe a static ['.tmux'] git-repo external for gpakosz/.tmux alongside the generated plugin blocks. No such block exists anymore in home/.chezmoiexternals/tmux.toml.tmpl (only the range .tmux_plugins.extras loop), and home/dot_config/tmux/tmux.conf:2 says the framework was explicitly replaced. The ADR and data-file comments were never updated after that migration, so they document a config that no longer exists. Fix: update ADR 0002 and the tmux-plugins.yaml comments to reflect the current externals-based setup, or file a superseding ADR.","status":"open","priority":3,"issue_type":"task","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:14Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:24:14Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-8d1","title":"ghostty config.tmpl has duplicate directives and dead chezmoidata fields","description":"home/dot_config/ghostty/config.tmpl:89-92 repeats 'cursor-style = block' / 'cursor-style-blink = true' verbatim back-to-back (copy-paste artifact). Separately, config.tmpl:62-69 sets background-opacity/background-blur from home/.chezmoidata/ghostty.yaml's background_opacity/background_blur fields, then a few lines later unconditionally hardcodes both to 0.96/20 inside the darwin branch — since darwin is the primary/only real target, those two data-file fields are dead and misleading. Fix: remove the duplicate cursor-style lines, and either make the darwin values respect the data file or remove the now-unused data fields.","status":"open","priority":3,"issue_type":"bug","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:13Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:24:13Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-96d","title":"Claude API key has two divergent 1Password ref sources (chezmoi prompt vs aichat config)","description":"home/.chezmoi.toml.tmpl prompts for and stores credentials.claude_api (a 1Password ref), consumed by home/.chezmoidata/aliases.yaml's credentials entry and home/dot_zshrc.tmpl:229-234. Separately, home/dot_config/aichat/config.yaml.tmpl:4 hardcodes a different literal ref: op://Private/Claude Dotfiles API Key/key. If the user's actual 1Password item path differs from that literal, aichat's API key silently breaks while the shell alias still works, or vice versa. Fix: have aichat/config.yaml.tmpl consume the same .credentials.claude_api templated value instead of a separate hardcoded literal.","status":"open","priority":3,"issue_type":"bug","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:13Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:24:13Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-77q","title":"docs/tmux.md and ADR-0002 describe the removed gpakosz/.tmux architecture","description":"docs/tmux.md (the whole file) documents tmux as built on gpakosz/.tmux vendored via a Chezmoi external at .tmux (type = git-repo), with ~/.tmux.conf symlinked into it, home/dot_tmux.conf.local for overrides, and TPM plugin support. None of this exists anymore: home/dot_config/tmux/tmux.conf line 2 states outright 'Replaces gpakosz/.tmux framework with explicit settings + tmux-sensible', there is no .tmux external anywhere in home/.chezmoiexternals/, and there is no dot_tmux.conf.local file. Plugins are now tmux-sensible/tmux-powerline/tmux-prefix-highlight loaded via run-shell, declared in home/.chezmoidata/tmux-plugins.yaml and fetched as individual git-repo externals (home/.chezmoiexternals/tmux.toml.tmpl).\\n\\ndocs/adrs/0002-tmux-plugins-via-chezmoi-externals.md's context section rests on the same now-false premise ('This repo already vendors the base gpakosz/.tmux layout...') and is marked status: accepted with no superseded note, even though the architecture it's reasoning about no longer exists.\\n\\nThis is a real risk for both humans and coding agents: docs/tmux.md is the doc AGENTS.md-adjacent tooling would point someone to for tmux config, and it currently describes files/behavior (dot_tmux.conf.local, the .tmux external, TPM) that don't exist, which would send anyone troubleshooting tmux down a dead end. Fix: rewrite docs/tmux.md to describe the current tmux-sensible + individually-pinned-plugin architecture, and add a superseded/updated note to ADR-0002's context (or a new ADR) reflecting the gpakosz removal.","status":"open","priority":3,"issue_type":"bug","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T19:20:00Z","created_by":"Meagan Waller","updated_at":"2026-07-24T19:20:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-f14","title":"bin/setup's mise install exit code is unreliable as a gate before bin/test","description":"bin/setup runs 'exec mise install' from the repo root, which mise resolves against BOTH the local root mise.toml (bats-core, gitleaks, shellcheck, etc -- the actual test/lint dependencies) AND the merged global user config (home/dot_config/mise/config.toml.tmpl's much larger tool list). If any tool in that merged set fails -- observed in a container: aqua:o2sh/onefetch@latest ('unsupported env: linux/arm64', not a real bug, just missing on that one arch) and npm:@google/gemini-cli@latest ('no version of @google/gemini-cli matches 0.0.8999999999999999', a broken/malformed npm-published version, arch-independent) -- mise install exits 1 overall, even though the tools actually needed (bats-core included) installed successfully and bin/test passes all 73 tests cleanly when run anyway. AGENTS.md documents the workflow as 'Run via ./bin/test after ./bin/setup', which reads naturally as bin/setup \u0026\u0026 bin/test -- but chaining with \u0026\u0026 makes bin/test never run whenever any unrelated global-config tool is broken. Discovered while validating the new Dockerfile/docker.yml (dotfiles-au4); worked around there by not chaining with \u0026\u0026. Two independent angles worth considering: (1) gemini-cli's broken npm version specifically -- verify whether it's a transient upstream npm state or a stale/wrong version pin in home/dot_config/mise/config.toml.tmpl; (2) more generally, bin/setup's exit code probably shouldn't gate bin/test at all, or bin/test's own docs/CI usage should stop chaining them with \u0026\u0026.","status":"open","priority":3,"issue_type":"bug","owner":"meagan.waller@gusto.com","created_at":"2026-07-24T17:38:32Z","created_by":"Meagan Waller","updated_at":"2026-07-24T17:38:32Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-phu","title":"private_dot_ssh/private_config.tmpl has asymmetric darwin/linux Host-block structure","description":"home/private_dot_ssh/private_config.tmpl:4-8 — the darwin branch wraps IdentityAgent in an explicit 'Host *' block, while the else-if eq .chezmoi.os \"linux\" branch emits a bare, indented IdentityAgent/HashKnownHosts pair with no Host line. Functionally equivalent today since both precede any Host stanza (so both apply globally), but the asymmetry is a latent trap if a Host-scoped block is ever added above it later — the linux branch's directives could then unintentionally apply only within the wrong scope, or fail to be recognized as global. Fix: wrap the linux branch in an explicit 'Host *' block too, matching the darwin branch's structure.","status":"open","priority":4,"issue_type":"task","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:18Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:24:18Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-dc5","title":"Extract duplicated CI/GITHUB_ACTIONS skip guard into a .chezmoitemplates partial","description":"The same CI-skip guard (checking CI/GITHUB_ACTIONS env vars before running) is duplicated with inconsistent styles across run_once_install-claude-code.sh:14 ([ ] style), run_onchange_after_install-sketchybar-lua.sh.tmpl:20, run_onchange_install-nvim-plugins.sh.tmpl:15, run_onchange_configure-macos-preferences.sh.tmpl:10 (mix of [ ] and [[ ]]), and run_onchange_install-packages-darwin.sh.tmpl:14,28 which reimplements the same check a third way at template-render time ({{ if or (env \"CI\") (env \"GITHUB_ACTIONS\") }}). There is no home/.chezmoitemplates/ directory today. Fix: add a .chezmoitemplates partial (or a small shared shell snippet sourced by the scripts) so the guard is defined once with one consistent style.","status":"open","priority":4,"issue_type":"task","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:17Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:24:17Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-l6x","title":"home/.chezmoiexternals/ghostty.toml lacks the .tmpl suffix its siblings have","description":"home/.chezmoiexternals/ghostty.toml is the only file in that directory without a .tmpl suffix even though its siblings (fish.toml.tmpl, sketchybar-app-font.toml.tmpl, sketchybar-lua.toml.tmpl, tmux.toml.tmpl, zsh.toml.tmpl) are all templates. Harmless today, but inconsistent and would need renaming the moment ghostty.toml needs an OS/host conditional. Fix: rename to ghostty.toml.tmpl for consistency (chezmoi will still render it fine with no template directives).","status":"open","priority":4,"issue_type":"task","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:17Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:24:17Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dotfiles-j0d","title":"Add a .chezmoiversion file to pin minimum chezmoi version","description":"Nothing under home/ pins a minimum chezmoi version, despite relying on relatively recent chezmoi features: stdinIsATTY, promptStringOnce/promptBoolOnce, onepasswordRead, and [onepassword] cache = true in home/.chezmoi.toml.tmpl. On an older chezmoi install these fail with cryptic template errors instead of a clear 'chezmoi version too old' message. Fix: add a .chezmoiversion file at the source root with the minimum required version.","status":"open","priority":4,"issue_type":"task","owner":"meagan.waller@gusto.com","created_at":"2026-07-27T16:24:16Z","created_by":"Meagan Waller","updated_at":"2026-07-27T16:24:16Z","dependency_count":0,"dependent_count":0,"comment_count":0}
