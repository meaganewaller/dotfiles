{"id":"int-4a1eb6204b0a43653e25be6f5ab1d1a5","kind":"field_change","created_at":"2026-07-24T17:42:35.995713Z","actor":"Meagan Waller","issue_id":"dotfiles-au4","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Dockerfile + .devcontainer/devcontainer.json + docker.yml CI job implemented and validated (build + full test suite pass). Four unrelated pre-existing bugs found along the way tracked separately: dotfiles-xeo, dotfiles-b73, dotfiles-gyp, dotfiles-f14."}}
{"id":"int-f07e8cf3f9c70c90a407aaef09d6ec81","kind":"field_change","created_at":"2026-07-27T16:26:48.168724Z","actor":"Meagan Waller","issue_id":"dotfiles-zwj","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Removed stray trailing single-quote in home/dot_config/lazygit/config.yml.tmpl:15 that broke the aicommit.sh path; verified via chezmoi execute-template that the rendered command now points to the correct script."}}
{"id":"int-a6563430a62eb983046da951ab0a2345","kind":"field_change","created_at":"2026-07-27T20:22:58.42199Z","actor":"Meagan Waller","issue_id":"dotfiles-1eb","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Removed the claude-chill custom alias from aliases.yaml (it always won over the claude() function in zsh, silently disabling the --add-dir variadic-argument fix) and folded claude-chill wrapping into the claude() function itself via a template-computed $claudeCmd prefix, gated by the same lookPath check. Verified via chezmoi execute-template + zsh -n; full BATS run blocked by an unrelated sandbox limitation (bash here-strings hang in this session for any .bats file)."}}
{"id":"int-2e45ecee32597daa2ead7b24e944958b","kind":"field_change","created_at":"2026-07-27T21:36:38.446426Z","actor":"Meagan Waller","issue_id":"dotfiles-xeo","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Fixed ./install's download_chezmoi to verify chezmoi's Sigstore bundle (chezmoi_<version>_checksums.txt.sigstore.json) via 'cosign verify-blob --bundle ... --key chezmoi_cosign.pub ...' instead of the retired detached .sig scheme. Confirmed via chezmoi's .config/goreleaser.yaml that it's still key-based signing (same cosign.key), just wrapped as a Sigstore bundle -- no OIDC/keyless verification needed. Removed the VERIFY_SIGNATURES=false workaround from Dockerfile now that real verification works. Validated end-to-end: (1) isolated shell harness sourcing download_chezmoi against the real v2.71.1 release, both with signatures on ('Verified OK') and off; (2) full Docker build of the Ubuntu container (no chezmoi/cosign apt package, forcing the GitHub-release+cosign fallback) succeeded with VERIFY_SIGNATURES defaulting to true, and the resulting image has a working goreleaser-built chezmoi binary."}}
{"id":"int-4d172b014c07a841557f8ebbf26cade6","kind":"field_change","created_at":"2026-07-28T14:44:31.30249Z","actor":"Meagan Waller","issue_id":"dotfiles-ei9","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Added 5 custom regex managers to renovate.json5 (aqua/github/ubi/npm/pipx-prefixed tools) scoped to home/dot_config/mise/config.toml.tmpl, since that file is a Chezmoi template (invalid TOML) that Renovate's native mise manager can never parse regardless of managerFilePatterns -- confirmed left the base mise: manager untouched intentionally, documented why. currentValue patterns require a leading digit so 'latest' entries (the deliberate ADR-0003 policy) are never touched. Fixed the wrong undotted config.toml path across docs/renovate.md, docs/package-management.md, docs/agents/chezmoi.md, README.md, docs/adrs/0003-mise-config-plus-lockfile.md, .claude/agents/package-manager.md, home/dot_claude/skills/install-tool/SKILL.md, and .claude/skills/install/SKILL.md (the last of which had a functional bug: its allowed-tools Edit() permission targeted the nonexistent undotted filename). Validated by parsing renovate.json5 as JS/JSON5, compiling every matchStrings regex, and running the 5 new managers against the real config file -- confirmed correct extraction of the 2 currently-pinned entries (aqua:pnpm/pnpm, ubi:miltonparedes/kitmux) and 0 false matches against latest/inline-table entries. Work was started by a Package Manager subagent (renovate.json5 + the 3 docs it touched) that died to a connection error before closing the issue; I verified its output and finished the remaining stale-path cleanup myself."}}
{"id":"int-46aeabea9d8c8ceced4d72ca9bf940b9","kind":"field_change","created_at":"2026-07-28T15:04:44.73797Z","actor":"Meagan Waller","issue_id":"dotfiles-zpg","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Both halves of this issue were resolved as a side effect of dotfiles-ei9: (1) the documented aqua/github/npm/pipx custom Renovate managers for home/dot_config/mise/config.toml.tmpl now actually exist in renovate.json5 (plus a bonus ubi: manager); (2) docs/package-management.md's Core Philosophy was rewritten from 'Immutable Pins Over Latest' to 'Reproducible Resolution' -- explicit versions OR latest-backed-by-a-committed-lockfile -- so the dozens of intentional 'latest' pins (policy per ADR 0003) no longer contradict the stated philosophy; they're now documented as the expected/correct state rather than a gap. Bulk-converting the ~150 entries to explicit pins was one of two alternative fixes the issue offered ('either add the documented custom managers... or update the docs to match reality') -- we did the latter (plus the former), which fully satisfies the fix criteria without the version-bump risk of touching every tool pin. Verified by re-reading both docs files post-ei9."}}
{"id":"int-a2059454fee7343ae9a83050e15f520f","kind":"field_change","created_at":"2026-07-28T15:06:43.109015Z","actor":"Meagan Waller","issue_id":"dotfiles-rxb","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Moved the mise activation block (and cargo PATH prepend) out of the {{ else }} (personal-profile) branch in home/dot_config/fish/config.fish.tmpl so it runs unconditionally after the work/personal PROFILE branch, instead of only on personal-profile machines. Verified by rendering the template with both work_profile=true and work_profile=false via chezmoi execute-template -- mise activate fish --shims | source now appears in both outputs -- and syntax-checked the rendered work-profile output with fish --no-execute."}}
{"id":"int-e6791786131cc58ea93ab20331dac4f5","kind":"field_change","created_at":"2026-07-28T15:09:03.859489Z","actor":"Meagan Waller","issue_id":"dotfiles-b73","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"install_chezmoi()'s final verification checked 'command -v chezmoi', which always fails on the GitHub-fallback path in non-interactive shells since $BIN_DIR isn't added to PATH until add_to_path() runs later in main() -- well after install_chezmoi returns. Since download_chezmoi already exit-1's internally on any real download/verify failure, reaching this check means the binary is guaranteed to exist at $BIN_DIR/chezmoi; changed the check to test file existence there directly instead of relying on PATH (main() already does its own more thorough PATH-independent candidate search afterward, so no need to duplicate that here). Verified with two isolated shell-harness runs against the real v2.71.1 release, PATH deliberately excluding $BIN_DIR and with a sanity check confirming chezmoi was truly unresolvable via command -v: the pre-fix code failed with exit 1 ('binary not found in PATH') despite a working binary sitting right at $BIN_DIR/chezmoi; the post-fix code returns exit 0."}}
{"id":"int-c811554de9ed6c569756f168fa8f24a1","kind":"field_change","created_at":"2026-07-28T15:12:45.26703Z","actor":"Meagan Waller","issue_id":"dotfiles-gyp","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Added packages.linux.apt to home/.chezmoidata/packages.yaml mirroring the dnf list (ca-certificates, gh, git, git-lfs, htop, neovim, tmux, zsh), fixing run_onchange_install-packages-linux.sh.tmpl's apt-get install -y branch which previously ran with zero packages (index .packages.linux \"apt\" returned nil, default list fell back to empty). Omitted util-linux-user with an explanatory comment: verified via apt-cache policy in a real ubuntu:24.04 container that it has no apt equivalent, and its user-management utilities (chsh, newgrp, etc.) already ship in Debian/Ubuntu's default util-linux/passwd/login packages (confirmed already 'Installed' on a bare image), unlike Fedora/RHEL where dnf's package needs explicit install. Verified the other 8 package names all resolve to real apt candidates in the same container, validated the YAML with yq, and rendered+shellchecked the full script template with the real data to confirm the apt-get install -y line now lists all 8 packages with correct bash syntax."}}
{"id":"int-73a87989970654fd2e5a85d37695908e","kind":"field_change","created_at":"2026-07-28T15:17:10.855873Z","actor":"Meagan Waller","issue_id":"dotfiles-d9h","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Changed run_onchange_00-install-mise-tools.sh.tmpl's missing-mise guard from 'exit 0' (silent skip) to 'exit 1' (real failure). Root cause: this script is run_onchange_ (content-hash gated), and chezmoi only records a script's hash as successfully-applied on exit 0 -- exiting 0 when mise isn't found yet (e.g. on a fresh machine where install-packages-darwin/linux, which installs mise itself, sorts after this script alphabetically) permanently marks the step done and it never retries until the tracked mise config/lockfile hash changes again. Verified the retry semantics with an isolated standalone chezmoi source/destination experiment (unrelated to the real repo): a run_onchange_ script that exits 1 gets re-run on every subsequent chezmoi apply with an unchanged hash, while one that exits 0 does not -- confirming this fix gives genuine self-healing retry behavior rather than a permanent skip. Verified the actual rendered script separately (mise excluded from PATH, run directly, no chezmoi apply involved) now exits 1 with a clear message instead of silently exiting 0. Note: an earlier test step in this session accidentally ran real 'mise install'/'mise prune' against the live environment (not properly isolated) and pruned one stale, already-unreferenced uv version -- flagged to the user directly; confirmed harmless via mise prune --dry-run showing nothing further pending."}}
{"id":"int-8416c0e77ed003686acd4c7f275b1585","kind":"field_change","created_at":"2026-07-28T15:21:13.615121Z","actor":"Meagan Waller","issue_id":"dotfiles-7tk","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Fixed the jsonata customManager query to match tmux-plugins.yaml's real shape: tmux_plugins.extras.{ depName (owner/repo via $replace-stripped url), currentValue: branch, currentDigest: revision } instead of the old tmuxPlugins.{repo,ref,commit} query that matched zero real fields. Added a per-entry branch field to the YAML since erikw/tmux-powerline's default branch is main while the other two are master (verified via gh api repos/<owner>/<repo> --jq .default_branch for all three) -- a single hardcoded currentValueTemplate would silently miss tmux-powerline. Independently verified: JSON5+regex validity via node eval, hand-simulated the $replace($replace(...)) depName extraction in plain JS against the real 3 YAML entries (all 3 produce correct depName/currentValue/currentDigest/packageName), and confirmed all three branch values against GitHub's actual default_branch. renovate-config-validator/renovate CLI unavailable locally to run the literal extract dry-run. Work done by a Package Manager subagent (no Bash access); I ran the validation and bd close on its behalf after independently re-verifying its claims."}}
{"id":"int-403557ec9e34e28328479edcbae39a95","kind":"field_change","created_at":"2026-07-28T15:21:14.534662Z","actor":"Meagan Waller","issue_id":"dotfiles-t8u","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Repointed all 4 dead customManagers (catppuccin/ghostty, ohmyzsh, zsh-autosuggestions, zsh-syntax-highlighting) from the nonexistent singular home/.chezmoiexternal.toml.tmpl to the real plural-directory paths (home/.chezmoiexternals/ghostty.toml, home/.chezmoiexternals/zsh.toml.tmpl). Re-pinned the 3 zsh-related externals from floating archive/master.tar.gz URLs to commit SHAs matching ghostty.toml's existing pattern -- independently verified all 3 SHAs against gh api repos/<owner>/<repo>/commits/master --jq .sha, all matched exactly. Confirmed via grep that renovate.json5 no longer references the singular chezmoiexternal.toml.tmpl path anywhere. Work done by a Package Manager subagent (no Bash access); I ran validation and bd close on its behalf after independently re-verifying the SHAs and path fixes myself."}}
{"id":"int-1d2a579a27c0d77b8724b7e776855c84","kind":"field_change","created_at":"2026-07-28T15:26:53.372903Z","actor":"Meagan Waller","issue_id":"dotfiles-dnd","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Re-pinned all 4 unpinned URLs in home/.chezmoiexternals/fish.toml.tmpl (chezmoi completions, watchexec completions, and 4 tokyonight.nvim theme files sharing one repo/SHA) from floating master/main branch refs to verified commit SHAs, matching the zsh/ghostty pattern already fixed in dotfiles-t8u. Verified each resolved SHA via gh api repos/<owner>/<repo>/commits/<branch> --jq .sha, then confirmed the exact file still exists at that SHA via HTTP HEAD against raw.githubusercontent.com for all 6 URLs (200 for all). Added 3 new customManagers to renovate.json5 (twpayne/chezmoi, watchexec/watchexec, folke/tokyonight.nvim) so these SHAs get automated future updates -- this file previously had zero Renovate coverage. Validated via node eval (JSON5 + regex compile) and by running the new regexes against the real post-edit file: chezmoi and watchexec each match once, tokyonight.nvim matches all 4 occurrences (correctly grouped into one future PR since they share depName). Validated the rendered TOML via chezmoi execute-template + yq -p toml. Documented the 3 new rules in docs/renovate.md's Chezmoi Externals section without attempting the full singular-vs-plural path sweep tracked separately by dotfiles-6oq."}}
{"id":"int-04712b792ca32d049151f5541be5e1b3","kind":"field_change","created_at":"2026-07-28T16:18:57.720546Z","actor":"Meagan Waller","issue_id":"dotfiles-7gl","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Confirmed the HEAD bug against Renovate's actual git-refs datasource source (lib/modules/datasource/git-refs/index.ts getDigest): when newValue is passed (Renovate always passes currentValue as newValue for digest lookups), it only matches refs with type 'heads'/'tags'; the real HEAD entry has type '' and is only matched when newValue is absent entirely. So currentValueTemplate: 'HEAD' can never resolve a digest -- confirmed, not just 'likely'.\n\nSince nvim-pack-lock.json is generated/rewritten by Neovim's vim.pack manager (confirmed via vim.pack.add() usage in home/dot_config/nvim/plugin/*.lua), it can't carry a per-entry branch field the way tmux-plugins.yaml does (that file is repo-owned). Converted the customManager from regex to jsonata: $each(plugins, ...) iterates the plugins object and looks up each repo's real default branch from a hardcoded table (all 26 repos' owner/repo -> default_branch verified via `gh api repos/<owner>/<repo> --jq .default_branch`; mix of main/master, e.g. kanagawa.nvim/lualine.nvim/melange-nvim/neverland-vim-theme/nvim-web-devicons/oil.nvim/onedark.nvim/vim-afterglow are master, rest are main).\n\nVerified: (1) renovate.json5 parses as valid JSON5 via a locally cached json5 module; (2) ran the real renovate-config-validator binary (found cached under ~/.npm/_npx from a prior npx run) against renovate.json5 -- validated successfully; (3) evaluated the literal JSONata expression via a locally cached jsonata@2.2.1 library against the real nvim-pack-lock.json content -- produced all 26 correct {depName, packageName, currentDigest, currentValue} entries matching the file and the verified default branches. Known limitation documented in a code comment: a new plugin added to nvim-pack-lock.json without a matching table entry will silently lack currentValue (no digest tracking) until the table is updated -- inherent to the lock file having no branch data of its own."}}
{"id":"int-7cf86b5c1adcb6bdfc7469898b6835c8","kind":"field_change","created_at":"2026-07-28T17:15:06.161187Z","actor":"Meagan Waller","issue_id":"dotfiles-sye","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Added BATS coverage for all 4 previously-untested chezmoiscripts, following the existing render-with-chezmoi-execute-template / stub-and-run patterns in test/test_helper.bash and the other test/*.bats files:\n\n- test/run_once_install-claude-code.bats (4 tests): valid shell structure, CI skip without invoking curl, already-installed skip without invoking curl, and a real curl|bash install path exercised with a fully isolated PATH (env -i) so it can't accidentally find a real claude/curl on the host.\n- test/run_onchange_after_install-sketchybar-lua.bats (8 tests): darwin-only rendering, CI skip, missing chezmoi-external-source skip, missing-make skip, successful build (stubbed make creates the .so), and the exit-1 path when make succeeds but the module never appears. Needed --source (not just --config's sourceDir data value) since the template's hash comment calls `include`, which chezmoi always resolves against the real -S/--source flag.\n- test/run_onchange_after_verify-theme-registry.bats (8 tests): this is the \"validation gate\" the issue specifically called out as needing regression coverage. Built a trimmed real-file fixture (only the specific files the template's hardcoded include list needs, skipping the 28MB unrelated bulk of dot_config) and exploited the fact that .chezmoi.sourceDir (template *data*, settable via --config) and the real --source flag (used by `include`) are independently controllable to test the missing-applier-dir and empty-applier-dir failure paths without needing a second full fixture tree. Also covered: missing-applier-in-palette and orphan-applier-in-directory failure paths (via yq -i mutation / touch), state-file-driven vs default-driven theme seeding, and a \"the real repo registry is currently self-consistent\" regression canary that will fail if the real repo's theme.d appliers and themes.yaml palette ever drift apart.\n- test/run_onchange_install-nvim-plugins.bats (8 tests): valid shell structure, template idempotence, CI skip, missing-nvim skip, successful sync (asserts nvim invoked with `--headless +Lazy! sync +qa`), failure propagation when nvim exits non-zero, and DEBUG=1 verbose output. Deliberately did NOT assert the hash comment's literal value, since (discovered while writing this) the glob path it hashes no longer exists post-vim.pack-migration, making the hash a constant -- filed as a separate bug (dotfiles-k7p) rather than baking the broken behavior into a passing assertion.\n\nAlso fixed a latent, previously-unexercised bug in test/test_helper.bash's assert_script_structure: it only substring-matched '#!/bin/bash' / '#!/bin/sh', which would have failed for any script using '#!/usr/bin/env bash' -- exactly the shebang style used by 2 of these 4 target scripts (theme-registry, nvim-plugins) and already used elsewhere in the repo (bin/sync-claude-extras, bin/sync-claude-settings). Extended it to also accept the env-indirected forms; verified against both old and new shebang styles before and after.\n\nVerified: all 28 new tests pass individually and the full suite (101 tests, up from 73) passes via `./bin/test` (the same entrypoint CI uses), with no regressions to the pre-existing 73.\n\nSide finding filed separately as dotfiles-k7p (not fixed here, out of scope for a test-coverage task): run_onchange_install-nvim-plugins.sh.tmpl's onchange hash and its `Lazy! sync` invocation are both stale relative to the vim.pack migration."}}
